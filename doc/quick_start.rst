

Quick start
===========

Clone the repository::

  $ git clone git@repo.ctcc.no:dalton.git

Build the code::

  $ cd dalton
  $ ./setup [--help]
  $ cd build
  $ make [-j4]

Run the test set::

  $ ctest [-j4]
